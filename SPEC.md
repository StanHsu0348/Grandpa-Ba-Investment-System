# 巴菲特「五點好企業原則」選股系統 — 開發規格書

> 本文件是給 Claude Code 的開發依據。請依此規格逐步建置一套可互動篩選台股清單的 Streamlit 應用程式。

---

## 1. 專案目標

以課程《巴菲特企業經營法》「濃縮版：5 點好企業原則」為核心邏輯，
讀入現成的台股清單 Excel（`twlist.xlsx`），做成一套**可互動調整篩選條件**的選股系統，
輸出符合五點原則（或部分原則）的候選股清單，並支援依產業類別篩選。

五點好企業原則：

| # | 原則 | 課程門檻 |
|---|---|---|
| 1 | 過去 5 年 ROE 是否穩定 | ROE > 15%，且 5 年穩定（非忽高忽低、非由高往下） |
| 2 | 配得出現金（低盈再率 + 高配息） | 配息率（Payout Ratio）> 40% |
| 3 | 不會變的公司 | 定性判斷：避開技術典範轉移快的產業（如電子代工），偏好品牌/民生/服務業 |
| 4 | 公司要夠大 | 淨利 > 5 億元（台灣）；上市櫃滿 2 年 |
| 5 | 老闆要有誠信 | 董監持股至少 10% |

延伸判準（附錄速查表）：稅前淨利國際級 > USD 75M；合理買價報酬率 IRR ≥ 15%。

---

## 2. 五點原則 → 資料欄位對照表（可執行邏輯）

系統目前只有 `twlist.xlsx`（台股）一份資料，欄位與五點原則對照如下：

| 原則 | 可否用現有資料計算 | 對應欄位 | 計算/篩選方式 |
|---|---|---|---|
| ① 5 年 ROE 穩定 | ✅ 可計算 | `ROE1`~`ROE5`, `預期ROE` | 見 3.1 節：每年 ROE ≥ 門檻（預設 15%，可調），並提供「穩定度」指標 |
| ② 配得出現金 | ✅ 可計算 | `預期配息率`（Payout Ratio）, `盈再率`（Retention Ratio） | `預期配息率 ≥ 門檻`（預設 40%），`盈再率` 顯示但不強制篩選（可能為負值或缺值） |
| ③ 不會變的公司 | ⚠️ 只能做半自動 | `SECTOR` | 提供產業類別（SECTOR）多選篩選器；系統另外標記「傳統定義上偏電子/景氣循環」的產業供使用者參考，但**不做強制排除**，交由使用者自行判斷 |
| ④ 公司夠大（淨利） | ✅ 可計算 | `預期常利`（單位：百萬元 NTD） | `預期常利 ≥ 500`（5 億元），門檻可調 |
| ④ 公司夠大（上市滿 2 年） | ❌ 資料無此欄位 | — | 標記「待人工查證」，UI 上顯示連結方便使用者去公開資訊觀測站查詢股票代號 |
| ⑤ 老闆誠信（董監持股 ≥10%） | ❌ 資料無此欄位 | — | 同上，標記「待人工查證」，預留欄位供未來串接資料 |

> 使用者已確認：④⑤ 的兩個缺失欄位先跳過不強制篩選，只用可計算的三項（①②④之淨利部分）做核心篩選邏輯，並在結果表格中加註「待人工查證」欄位。未來若取得董監持股與上市年資資料，可直接把資料放進 `data/` 目錄替換或合併。

---

## 3. 篩選邏輯細節

### 3.1 原則①：5 年 ROE 穩定度 — 提供多種模式並列，由使用者自行選擇

不預設單一「正確」判斷方式，改成把所有合理模式都做出來、都可勾選，並在旁邊即時顯示「符合家數」，讓使用者自己比較後決定要用哪一種（類似 Finviz Screener 每個篩選欄位旁邊即時顯示符合筆數的做法）。

**開發前置工作（Phase 1 必做）**：先確認 `ROE1`~`ROE5` 的時間順序（哪個是最近一年、哪個是 5 年前）。做法：挑一支資料中的知名股票（例如 2330 台積電），拿真實年度 ROE 對照 GoodInfo 或年報數字，確認順序後把結論寫進 `constants.py` 的註解，並在 `data_loader.py` 建立 `ROE_MOST_RECENT_COL`、`ROE_OLDEST_COL` 常數，避免後面模組猜錯方向。

**五種可選模式**（UI 上用 radio button 或 checkbox 群組並列，皆可調整數值門檻）：

| 模式 | 邏輯 | 適用情境 |
|---|---|---|
| A. 嚴格版 | `min(ROE1..ROE5) ≥ 門檻` | 最嚴格，符合課程字面「過去5年ROE是否穩定」原意，但候選數量最少 |
| B. 近 3 年版 | 近 3 年（依確認後的時間順序取值）`min(近3年ROE) ≥ 門檻` | 較寬鬆，避開較久遠年度雜訊，適合新上市或轉型後體質變好的公司 |
| C. 平均版 | `mean(ROE1..ROE5) ≥ 門檻` | 允許單年度表現不佳，但長期平均仍達標 |
| D. 預估值版 | `預期ROE ≥ 門檻` | 最寬鬆，只看最新一年預估值，適合快速初篩 |
| E. 趨勢版 | 用線性回歸（`numpy.polyfit`）判斷「一致向上」或「持平在門檻之上」，排除「由高往下」與「忽高忽低」 | 最貼近課程投影片「保持一致／一致向上」為佳、「景氣循環／由高往下／忽高忽低」應避免的精神 |

**穩定度輔助指標**（不論選哪個模式都同時顯示，供參考排序）：
- 標準差 `std(ROE1..ROE5)`：越低代表越穩定
- 趨勢分類標籤：一致向上／持平／由高往下／忽高忽低（供使用者肉眼判斷用，對應課程投影片分類）

**資料完整度**：`ROE4`、`ROE5` 缺值比例較高（分別約 1.5%、3.3%），缺值以 NaN 處理、不納入計算，並在結果中標示「資料不完整」，模式 A（嚴格版）遇到缺值應視為不通過（因為無法確認該年度是否達標），模式 B/C/D 則視實際可用年數計算並標示。

### 3.2 原則②：配得出現金

- `預期配息率 ≥ 配息門檻`：UI 用滑桿（0–100%，可自訂）＋常用預設值快捷按鈕（如 Finviz 常見的 `>20%`／`>40%`／`>60%`／`不限`，一鍵套用後仍可微調）
- `盈再率` 顯示於結果表格中作為輔助參考（盈再率負值代表公司配息超過當年獲利，或財報有特殊狀況，需留意）
- 缺值（約 590 筆 `盈再率` 為 null）不影響配息率的篩選，只是輔助欄位留空

### 3.3 原則④：公司夠大（淨利）

- `預期常利 ≥ 淨利門檻`：UI 用數字輸入框（單位百萬元）＋快捷按鈕（如 `≥100`／`≥500`（5億，課程門檻）／`≥1000`／`不限`），可自訂任意數值
- 「上市櫃滿 2 年」目前無資料可判斷，UI 顯示提示文字，不做篩選

> 以上三項篩選（ROE、配息率、淨利）的 UI 呈現方式參考 Finviz Screener（finviz.com/screener）每個欄位「滑桿/下拉選單 + 即時顯示符合筆數」的設計，但門檻完全開放使用者自訂，不強制套用課程或 Finviz 的預設值。

### 3.4 原則③⑤：定性/缺資料項目

- SECTOR 篩選器：多選下拉選單（30 個產業類別，注意原始資料字串有補齊空白，需 `str.strip()`）
- 結果表格加一欄「五點覆蓋度」，例如顯示 `3/5`，明確告知使用者這是「可計算三項」的篩選結果，②④⑤仍需自行研究

### 3.5 加分項（選用）：估價區間

資料中已有 `貴價`（昂貴價）與 `淑價`（便宜價，對應課程 Ch6 IRR 折現估價概念）欄位，可作為選配的加分篩選：

- 「目前為便宜價區間」：`收盤價 ≤ 淑價`
- 「目前為合理價以下」：`淑價 < 收盤價 < 貴價`
- 「目前為昂貴價」：`收盤價 ≥ 貴價`

此為加分篩選，非五點原則的一部分，UI 上獨立呈現、可勾選開關。

### 3.6 加分項（選用）：預期報酬率（IRR）

> 開發過程補充：資料中另有 `預期報酬率` 欄位（原規格書field 對照表遺漏），對應第 1 節提到的延伸判準
> 「合理買價報酬率 IRR ≥ 15%」，數值範圍約 -51%~57%，593 筆缺值（無法估算）。

- 篩選邏輯：`預期報酬率 ≥ 門檻`（預設門檻 15%，對應課程延伸判準），UI 用開關＋滑桿呈現，預設不套用
- 缺值（無法估算 IRR）於套用門檻時一律排除
- 此為加分篩選，非五點原則的一部分，與估價區間篩選同樣獨立呈現、可勾選開關

---

## 4. 資料清洗注意事項（開發時務必處理）

1. **SECTOR 欄位有補齊空白**：讀入後需 `df['SECTOR'] = df['SECTOR'].str.strip()`
2. **`財報幣別`**：台股清單全部為 `NTD`，不需轉換，但程式仍應保留此欄位判斷邏輯以利未來擴充美股（`uslist_2.xlsx` 有多種幣別：USD、EUR、CNY、CAD 等）
3. **缺值處理**：
   - `ROE3`：7 筆缺值
   - `ROE4`：31 筆缺值
   - `ROE5`：68 筆缺值
   - `盈再率`：590 筆缺值（缺值比例較高，只作顯示不作篩選依據）
   - `貴價`/`淑價`：約 579 筆缺值（新股或資料不足者），估價篩選需將缺值排除或標示「無法估價」
4. **數值單位**：
   - ROE 相關欄位已是百分比數值（例如 `15.0` 代表 15%），不需再乘 100
   - `預期常利`（淨利）單位為「百萬元」，5 億元對應數值為 `500`
5. **異常值**：`預期常利` 有極端負值（如 -12,302.7 百萬元），代表公司嚴重虧損，篩選時自然會被淨利門檻排除，不需特別處理

---

## 5. 系統架構

### 5.1 技術選型

- **語言**：Python 3.11+
- **介面框架**：Streamlit（互動式網頁 App，本機執行 `streamlit run app.py`）
- **資料處理**：pandas
- **圖表**：Streamlit 內建 `st.line_chart` / `st.bar_chart`，或 Plotly（呈現 5 年 ROE 趨勢圖）

### 5.2 專案目錄結構

```
buffett-stock-screener/
├── app.py                     # Streamlit 主程式（UI 入口）
├── data/
│   └── twlist.xlsx          # 台股原始資料（未來可加 uslist_2.xlsx）
├── src/
│   ├── __init__.py
│   ├── data_loader.py         # 讀檔、清洗、欄位標準化
│   ├── screener.py            # 五點原則篩選邏輯（純函式，方便單元測試）
│   ├── scoring.py             # ROE 穩定度評分、五點覆蓋度計算
│   └── constants.py           # 預設門檻值、SECTOR 對照表等常數
├── tests/
│   └── test_screener.py       # 用課程範例公司做為測試案例（見第 8 節）
├── requirements.txt
└── README.md
```

### 5.3 模組職責

- `data_loader.py`
  - `load_tw_data(path) -> pd.DataFrame`：讀檔、去除 SECTOR 空白、型別轉換
  - 回傳統一欄位命名的 DataFrame，供後續模組使用
- `screener.py`
  - `filter_by_roe(df, threshold=15.0) -> pd.DataFrame`
  - `filter_by_payout(df, threshold=40.0) -> pd.DataFrame`
  - `filter_by_net_income(df, threshold=500) -> pd.DataFrame`
  - `filter_by_sector(df, sectors: list[str]) -> pd.DataFrame`
  - `filter_by_valuation(df, mode: Literal["cheap","fair","expensive","any"]) -> pd.DataFrame`
  - `apply_all_filters(df, params: dict) -> pd.DataFrame`：串接以上函式
- `scoring.py`
  - `compute_roe_stability(row) -> dict`：回傳標準差、趨勢分類（一致向上/持平/由高往下/忽高忽低）
  - `compute_coverage_score(row) -> str`：回傳如 `"3/5"` 的五點覆蓋度字串
- `constants.py`
  - `DEFAULT_ROE_THRESHOLD = 15.0`
  - `DEFAULT_PAYOUT_THRESHOLD = 40.0`
  - `DEFAULT_NET_INCOME_THRESHOLD = 500`
  - `MANUAL_CHECK_NOTE = "董監持股與上市年資需自行至公開資訊觀測站查證"`

---

## 6. UI 設計（Streamlit 頁面配置）

### 6.1 側邊欄（篩選條件區）

```
【側邊欄 st.sidebar】
├─ 📁 資料來源區（見 6.3 節）
│     └─ 上傳新的 Excel／或使用 data/ 目錄中的既有檔案，並顯示資料日期
│
├─ ① ROE 穩定度篩選（5 種模式並列，可複選比較）
│     ├─ 模式選擇：☐ A.嚴格版　☐ B.近3年版　☐ C.平均版　☐ D.預估值版　☐ E.趨勢版
│     │     （每個模式勾選後才會出現對應的門檻滑桿，未勾選則不套用該條件）
│     ├─ 各模式共用 ROE 門檻滑桿：0–40%，無預設值，使用者自行設定
│     └─ 即時顯示：「目前條件下符合家數：XX 家」
│
├─ ② 配息率篩選
│     ├─ 門檻滑桿：0–100%
│     └─ 快捷按鈕：>20% / >40% / >60% / 不限
│
├─ ④ 淨利門檻
│     ├─ 數字輸入框（百萬元），使用者自訂
│     └─ 快捷按鈕：≥100 / ≥500(5億) / ≥1000 / 不限
│
├─ ③ 產業類別多選（SECTOR）：預設全選，可搜尋，供使用者自行判斷「是否為不易變的產業」
│
├─ 加分：估價區間篩選（便宜價／合理價／昂貴價／不限）
│
├─ 【清空所有篩選】按鈕（不做「課程預設值」按鈕，因為使用者要自行設定並比較所有模式）
```

### 6.2 主畫面

```
【主畫面】
├─ 上方指標卡（st.metric）：符合條件家數 / 資料總家數 / 平均ROE
├─ 結果表格（st.dataframe，可排序）
│   欄位：股票代號｜公司名稱｜產業｜預期ROE｜5年ROE(mini趨勢)｜配息率｜淨利(億)｜估價區間｜五點覆蓋度
├─ 點選任一列 → 展開詳細卡片（st.expander）
│   ├─ 5年 ROE 折線圖
│   ├─ 該公司對應五點原則逐項檢核表（✅/⚠️/❌ 待查證）
│   └─ 「④上市年資、⑤董監持股請至公開資訊觀測站查證」連結按鈕
│       （連結格式：https://mops.twse.com.tw/mops/web/t51sb01?TYPEK=sii&co_id={股票代號}）
├─ 下載按鈕：匯出篩選結果為 CSV / Excel
```

### 6.3 資料更新機制（因為 Excel 會不定期更換）

提供**兩種更新方式並存**，使用者用哪種都可以：

1. **檔案總管方式（最單純）**：固定讀取 `data/twlist.xlsx` 這個路徑。使用者只要拿到新的 Excel，直接覆蓋這個檔案（保持檔名不變）即可，重新整理網頁後自動套用新資料，完全不用碰程式碼。
2. **App 內上傳方式（更方便）**：在側邊欄最上方加入 `st.file_uploader("上傳最新股票清單 Excel")`。使用者可直接把新的 Excel 拖進網頁，系統當次執行改用上傳的檔案，不影響 `data/` 目錄中的原始檔（適合想暫時測試新資料、不想覆蓋既有檔案的情境）。

兩種方式的優先順序：**若使用者有上傳檔案，優先使用上傳的；否則預設讀取 `data/twlist.xlsx`**。

主畫面上方需顯示目前使用的資料來源與資料日期（讀取 `收盤日` 欄位的最大值），讓使用者一眼看出目前分析的是哪一批資料，避免用舊資料做決策。

`data_loader.py` 中應把「欄位名稱、欄位順序」的容錯處理做好（例如用欄位名稱而非欄位位置索引讀取），這樣未來即使 Excel 欄位順序有微調，系統也不會讀錯。若新檔案的欄位名稱有變動或缺少必要欄位，App 應顯示明確錯誤訊息（例如「找不到『預期ROE』欄位，請確認檔案格式」），而不是直接崩潰。

---

## 7. 開發階段（建議 Claude Code 依序執行）

**Phase 1｜MVP（可先完成，確保核心邏輯正確）**
1. 建立專案骨架與 `requirements.txt`（pandas, streamlit, openpyxl）
2. `data_loader.py`：讀入 `twlist.xlsx`，清洗 SECTOR 空白，處理缺值
3. `screener.py`：實作①②④三項篩選函式 + `apply_all_filters`
4. `tests/test_screener.py`：用第 8 節的課程範例公司驗證邏輯正確
5. 最簡單的 `app.py`：側邊欄三個滑桿 + 一個結果表格，先跑得起來

**Phase 2｜完整互動介面**
6. 加入 SECTOR 多選篩選器、五點覆蓋度欄位
7. 加入 5 年 ROE 趨勢圖與穩定度分類（一致向上/持平/由高往下/忽高忽低）
8. 加入估價區間（貴價/淑價）加分篩選
9. 加入詳細卡片檢視、CSV/Excel 匯出功能

**Phase 3｜資料補強與美股擴充（之後再做）**
10. 若取得董監持股、上市年資資料，串接進系統，補齊④⑤
11. 支援 `uslist_2.xlsx`（美股），注意其 SECTOR/Industry 為英文分類、幣別多樣，需在 `data_loader.py` 增加幣別轉換或標示邏輯
12. 加入 IRR 計算機（對應課程 Ch6），讓使用者輸入預期持有年數、目標報酬率，計算合理買價

---

## 8. 測試案例（用課程投影片提到的公司驗證邏輯）

課程投影片「好公司範例清單」中提到以下公司，可作為單元測試的基準（用實際資料驗證篩選函式是否正確標記，而非要求都通過所有篩選 — 因為資料是最新的，數字會與投影片當時不同）：

| 股票代號 | 公司 | 目前 `預期ROE` | 目前 `預期配息率` | 目前 `預期常利`（百萬） | 備註 |
|---|---|---|---|---|---|
| 1216 | 統一 | 12.47% | 90.66% | 16,929.3 | ROE 略低於 15% 門檻，配息率高、淨利遠超門檻 → 可測試「部分符合」的顯示邏輯 |
| 6409 | 旭隼 | 32.42% | 94.98% | 3,097.0 | 應完全通過①②④三項篩選 |
| 2313 | 華通 | 14.65% | 64.97% | 7,009.7 | ROE 略低於 15%，測試門檻邊界情況 |
| 1752 | 南光 | 8.86% | 64.37% | 220.1 | ROE 與淨利皆低於門檻 → 應被篩掉，測試排除邏輯 |
| 4763 | （原「材料-KY」，資料中公司名稱已更新） | 26.95% | 61.04% | 5,362.7 | 應通過①②④三項篩選 |
| 8478 | 東哥（原「東哥遊艇」） | 14.27% | 56.94% | 1,292.5 | ROE 略低於 15%，測試邊界情況 |

> 建議測試除了驗證「用預設門檻篩選後這幾家公司是否如預期被納入/排除」之外，也要測試「調降 ROE 門檻至 10%」後，統一、華通、東哥是否會被納入 — 確保滑桿參數確實生效。

---

## 9. 已知限制（務必在 App 中明確告知使用者）

- 本系統**只能自動判斷五點原則中的 ①②④（淨利部分）**，③（不會變的公司）僅提供產業分類供人工判斷，④（上市滿 2 年）與 ⑤（董監持股 ≥10%）**完全無法自動判斷**，須自行至公開資訊觀測站或 GoodInfo 查證。
- 資料為單一時間點快照（`收盤日` 欄位可看出資料日期），非即時更新，需注意資料新舊。
- 「5 年 ROE 穩定」目前用 `min(ROE1..ROE5) ≥ 門檻` 做簡化判斷，這是嚴格版本（要求每年都達標），與課程真正想強調的「穩定而非多高」略有差異，Phase 2 會補上標準差/趨勢分類讓判斷更貼近課程精神。

---

## 10. 給 Claude Code 的執行建議

1. 先建立 Phase 1 骨架並跑通，讓使用者看到最小可行的篩選結果表格。
2. 每完成一個模組就用第 8 節的測試案例驗證一次，避免篩選邏輯出錯。
3. UI 文字一律使用繁體中文，維持與課程投影片用詞一致（例如「配息率」而非「殖利率」）。
4. `data/twlist.xlsx` 路徑之後若使用者提供更新資料，程式應可直接覆蓋讀取，不需改程式碼。
